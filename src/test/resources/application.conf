# Connection info for test database and schema.

db.test.driver=org.postgresql.Driver
db.test.url="jdbc:postgresql://127.0.0.1/debiki_test"
db.test.user=debiki_test
db.test.password="auto-deleted"

# Override above settings via this included file, if, for example, the database
# is located on another machine (not 127.0.0.1).
include "local/test-db.conf"

